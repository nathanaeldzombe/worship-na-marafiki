# Copy this file to .env.local for local development.
# In production, Vercel injects DATABASE_URL automatically when you
# connect a Postgres store — you do NOT set it manually there.

DATABASE_URL="postgres://user:password@host/dbname?sslmode=require"
