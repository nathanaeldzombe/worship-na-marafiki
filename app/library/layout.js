export const metadata = {
  title: 'Song Library — Browse African Worship Songs',
  description:
    'Browse and search African worship songs by title, language, theme, and tempo. Lyrics, ChordBora chords, and key transposition across 30+ languages.',
  alternates: { canonical: '/library' },
};

export default function LibraryLayout({ children }) {
  return children;
}
